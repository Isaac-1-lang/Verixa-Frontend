'use client';
import Link from 'next/link';import {GraduationCap,Plus,Users,CalendarDays,BookOpen,AlertTriangle,ArrowRight,RefreshCw,ClipboardCheck,MessageSquareWarning,CircleAlert} from 'lucide-react';
import {useGetMeQuery,useGetTrainingOverviewQuery,useGetTrainingProgramsQuery} from '@/app/redux/api/TrainingApiSlice';import {useTrainingScope} from './layout';
const rows=value=>value?.content||[];const nice=value=>String(value||'').replaceAll('_',' ').toLowerCase().replace(/(^|\s)\S/g,c=>c.toUpperCase());
export default function TrainingDashboardPage(){const me=useGetMeQuery();const organizationId=me.data?.organizations?.[0]?.id;const {projectId}=useTrainingScope();const overview=useGetTrainingOverviewQuery({organizationId},{skip:!organizationId});const programs=useGetTrainingProgramsQuery({organizationId,page:0,size:100},{skip:!organizationId});const allPrograms=rows(programs.data);const visible=projectId?allPrograms.filter(item=>String(item.projectId)===String(projectId)):allPrograms;const active=visible.filter(item=>item.status==='ACTIVE').length;const data=overview.data||{};const loading=me.isLoading||overview.isLoading||programs.isLoading;const error=me.error||overview.error||programs.error;const metrics=[{label:'Active programs',value:active,detail:projectId?'Selected project':'All projects',icon:BookOpen,href:'/dashboard/training/programs'},{label:'Participants',value:data.participants??0,detail:'Reusable organization roster',icon:Users,href:'/dashboard/training/participants'},{label:'Upcoming sessions',value:data.upcomingSessions??0,detail:`${data.completedSessions??0} completed`,icon:CalendarDays,href:'/dashboard/training/sessions'},{label:'Attendance rate',value:`${data.attendanceRate??0}%`,detail:'Across recorded assignments',icon:ClipboardCheck,href:'/dashboard/training/sessions'}];
 if(loading)return <div className="space-y-5 animate-pulse"><div className="h-44 rounded-2xl bg-navy/8"/><div className="grid gap-4 md:grid-cols-4">{[1,2,3,4].map(i=><div key={i} className="h-32 rounded-xl bg-navy/5"/>)}</div></div>;if(error)return <div className="rounded-2xl border border-red-200 bg-red-50 p-8 text-center"><AlertTriangle className="mx-auto text-red-500"/><h1 className="mt-3 text-xl font-bold text-navy">Training data could not be loaded</h1><p className="mt-2 text-sm text-navy/50">The server returned {error?.data?.message||'an unavailable response'}.</p><button onClick={()=>window.location.reload()} className="mt-5 inline-flex items-center gap-2 rounded-md bg-navy px-4 py-2 font-bold text-white"><RefreshCw size={15}/>Retry</button></div>;
 return <div className="space-y-6"><section className="rounded-2xl bg-navy p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-white/50"><GraduationCap size={16}/>Operational command center</div><h1 className="mt-4 text-3xl font-bold sm:text-4xl">Know who needs action—now</h1><p className="mt-3 max-w-2xl text-white/60">Live attendance, understanding, unresolved problems, ownership, and rollout readiness without combining spreadsheets.</p></div><Link href="/dashboard/training/programs" className="inline-flex items-center justify-center gap-2 rounded-lg bg-white px-5 py-3 font-bold text-navy"><Plus size={16}/>Create program</Link></div></section>
 <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({label,value,detail,icon:Icon,href})=><Link key={label} href={href} className="rounded-xl border border-navy/8 bg-white p-5 shadow-sm hover:shadow-md"><div className="flex justify-between"><span className="grid h-10 w-10 place-items-center rounded-lg bg-navy/5"><Icon size={18}/></span><ArrowRight size={15} className="text-navy/20"/></div><p className="mt-5 text-3xl font-bold text-navy">{value}</p><p className="mt-1 text-sm font-bold text-navy/60">{label}</p><p className="mt-2 text-xs text-navy/35">{detail}</p></Link>)}</div>
 <div className="grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><section className="rounded-xl border border-navy/8 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold text-navy">Needs attention</h2><p className="mt-1 text-xs text-navy/40">Calculated from live evidence across the organization</p></div><Link href="/dashboard/training/readiness" className="text-xs font-bold text-navy">Open readiness →</Link></div><div className="mt-5 grid grid-cols-3 gap-3"><Status value={data.readiness?.READY??0} label="Ready" tone="emerald"/><Status value={data.readiness?.FOLLOW_UP_REQUIRED??0} label="Follow-up" tone="amber"/><Status value={data.readiness?.NOT_READY??0} label="Not ready" tone="red"/></div><div className="mt-5 space-y-2">{data.topTrainingGaps?.length?data.topTrainingGaps.map(gap=><div key={gap.reason} className="flex items-center justify-between rounded-lg bg-navy/[.025] p-3"><p className="text-sm font-semibold text-navy/65">{gap.reason}</p><span className="rounded-full bg-navy/5 px-2.5 py-1 text-xs font-bold text-navy">{gap.count}</span></div>):<p className="rounded-lg bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">No evidence-based training gaps yet.</p>}</div></section>
 <section className="rounded-xl border border-navy/8 bg-white p-5"><div className="flex items-center gap-2"><MessageSquareWarning size={17}/><h2 className="font-bold text-navy">Issue accountability</h2></div><div className="mt-5 grid grid-cols-3 gap-3"><Status value={data.openIssues??0} label="Open"/><Status value={data.unassignedIssues??0} label="Unassigned" tone="amber"/><Status value={data.blockingIssues??0} label="Blocking" tone="red"/></div><div className="mt-5"><p className="text-xs font-bold uppercase tracking-wider text-navy/35">Groups needing attention</p><div className="mt-2 space-y-2">{data.groupsNeedingAttention?.length?data.groupsNeedingAttention.map(group=><Link href={`/dashboard/training/readiness`} key={`${group.programId}-${group.id}-${group.name}`} className="flex items-center justify-between rounded-lg border border-navy/7 p-3"><div><p className="text-sm font-bold text-navy">{group.name}</p><p className="text-xs text-navy/40">{group.programTitle}</p></div><span className="text-sm font-bold text-amber-700">{group.readinessPercent}%</span></Link>):<p className="text-sm text-navy/40">No groups currently require attention.</p>}</div></div></section></div>
 <section className="overflow-hidden rounded-xl border border-navy/8 bg-white"><div className="border-b border-navy/6 p-5"><h2 className="font-bold text-navy">Programs in scope</h2></div>{visible.length?<div className="divide-y divide-navy/5">{visible.slice(0,8).map(item=><Link key={item.id} href={`/dashboard/training/programs/${item.id}`} className="flex items-center justify-between p-5 hover:bg-navy/[.02]"><div><p className="font-bold text-navy">{item.title}</p><p className="mt-1 text-xs text-navy/40">{item.memberCount} members · {item.sessionCount} sessions</p></div><span className="rounded-full bg-navy/5 px-3 py-1 text-xs font-bold text-navy">{nice(item.status)}</span></Link>)}</div>:<div className="p-12 text-center"><CircleAlert className="mx-auto text-navy/15"/><p className="mt-3 font-bold text-navy">No programs in this scope</p></div>}</section></div>}
function Status({value,label,tone='navy'}){const colors={navy:'bg-navy/[.035] text-navy',emerald:'bg-emerald-50 text-emerald-700',amber:'bg-amber-50 text-amber-700',red:'bg-red-50 text-red-700'};return <div className={`rounded-lg p-3 ${colors[tone]}`}><p className="text-2xl font-bold">{value}</p><p className="mt-1 text-[11px] font-bold">{label}</p></div>}
